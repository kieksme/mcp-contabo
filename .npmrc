# Required for `pnpm deploy` (used by the Docker builds) to produce a
# self-contained output directory: workspace dependencies (e.g. dashboard's
# dependency on @kieksme/contabo-mcp) are copied in rather than symlinked.
inject-workspace-packages=true
