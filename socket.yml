version: 2

# Only run PR alerts when a package manifest changes. The lockfile now lives
# at the workspace root (pnpm workspace covering contabo-mcp/ + dashboard/).
triggerPaths:
  - "contabo-mcp/package.json"
  - "dashboard/package.json"
  - "pnpm-lock.yaml"
